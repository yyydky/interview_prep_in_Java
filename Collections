1. List
- ArrayList, LinkedList, Vector (thread safe)
- add(), remove(), clear(), get(), contains(), size()
- duplicates
- ordered

2. Set
- HashSet, LinkedHashSet, TreeSet (better for ordered key) 
- add(), remove(), clear(), contains(), size()
- no duplicate
- unordered (HashCode)

3. Map
- HashMap, HashTable (thread safe)
- put(), get(), remove(), clear(), containsKey(), containsValue(), keySet(), values(), size()
- no duplicate

4. Array vs. List
- List toArray => ArrayList
- Array asList => List

5. 


